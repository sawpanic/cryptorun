# Performance Report CSV Export
# Analysis Period: {{.StartDate}} to {{.EndDate}} ({{.TotalDays}} days)
# Generated: {{.GeneratedAt}}

## UX MUST — Live Progress & Explainability
# This CSV provides machine-readable performance metrics with full transparency

metric,value,threshold,status
spec_pnl,{{.SpecPnL}},0,{{if gt .SpecPnL 0}}PASS{{else}}FAIL{{end}}
raw_pnl,{{.RawPnL}},0,{{if gt .RawPnL 0}}PASS{{else}}FAIL{{end}}
net_pnl,{{.NetPnL}},0,{{if gt .NetPnL 0}}PASS{{else}}FAIL{{end}}
total_fees,{{.TotalFees}},0,INFO
total_slippage,{{.TotalSlippage}},0,INFO
total_return,{{.TotalReturn}},0.15,{{if gt .TotalReturn 0.15}}PASS{{else}}WARN{{end}}
annualized_return,{{.AnnualizedReturn}},0.15,{{if gt .AnnualizedReturn 0.15}}PASS{{else}}WARN{{end}}
sharpe_ratio,{{.Sharpe}},1.0,{{if gt .Sharpe 1.0}}PASS{{else}}FAIL{{end}}
max_drawdown,{{.MaxDrawdown}},0.20,{{if lt .MaxDrawdown 0.20}}PASS{{else}}FAIL{{end}}
hit_rate,{{.HitRate}},0.50,{{if gt .HitRate 0.50}}PASS{{else}}WARN{{end}}
profit_factor,{{.ProfitFactor}},1.0,{{if gt .ProfitFactor 1.0}}PASS{{else}}FAIL{{end}}
